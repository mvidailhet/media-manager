{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allows the main window to use app commands and open the folder picker for Scan Roots.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
